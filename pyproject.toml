[project]
name = "constitution-crew"
version = "0.1.0"
description = "Indian Constitution Query Assistant powered by CrewAI"
requires-python = ">=3.10"
dependencies = [
    "crewai",
    "crewai-tools",
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
]

[project.scripts]
start = "main:main"
